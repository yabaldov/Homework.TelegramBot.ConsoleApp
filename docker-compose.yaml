services:
  postgres_db:
    image: postgres:17-alpine
    container_name: pg_automation
    environment:
      POSTGRES_USER: qa_admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ToDoList
    ports:
      - "5432:5432"
    volumes:
      - pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qa_admin -d ToDoList"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pg:
